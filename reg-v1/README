Execute these commands to test the program:

    mkdir build
    cd build
    cmake ..
    make
    cd ..
    ./test-reg.sh
    
    
    or
    
    gcc-5.3.0 -o build/reg reg.c my_semblance.c su.c -I/opt/altera/14.1/hld/host/include/ -lOpenCL -lm -std=gnu99 -D_FILE_OFFSET_BITS=64 -D_LARGEFILE64_SOURCE
    
    or
    
    
    gcc -o build/reg reg.c -I. my_semblance.c su.c  -lOpenCL -lm -std=gnu99 -D_FILE_OFFSET_BITS=64 -D_LARGEFILE64_SOURCE     

The expected results for the parameters, semblance 
and stack are located in expected.out
